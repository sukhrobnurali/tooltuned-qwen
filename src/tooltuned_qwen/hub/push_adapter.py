"""Push the LoRA adapter folder to the HF hub."""


def push(adapter_path: str, repo: str, *, private: bool = False) -> str:
    """Upload `adapter_path` to `repo`. Returns the HF URL."""
    raise NotImplementedError("Phase 5.2")
